// rafce と入力後にタブキーを押す

import React from "react";

const Test = ({ gs, age, zz, bb }) => {
  return (
    <>
      <div>{gs}</div>
      <p>{age}</p>
      <p>{zz}</p>
      <h1>{bb}</h1>
    </>
  );
};

export default Test;
